# Default profile selects which config.*.yaml to load when not specified.
DEFAULT_PROFILE=paper

# Binance API credentials (testnet/live). Leave empty for paper simulations.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# OKX API credentials (testnet/live).
OKX_API_KEY=
OKX_API_SECRET=
OKX_PASSPHRASE=

# SAFE_MODE toggle (1 keeps SAFE_MODE enabled, 0 disables after confirmations).
SAFE_MODE=1
